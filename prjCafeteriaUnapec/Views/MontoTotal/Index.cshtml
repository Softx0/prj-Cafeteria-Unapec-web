@model IEnumerable<prjCafeteriaUnapec.MontoTotal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MONTOTOTAL1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MONTOTOTAL1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idfactura }) |
            @Html.ActionLink("Details", "Details", new { id=item.idfactura }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idfactura })
        </td>
    </tr>
}

</table>
